# ── 핵심 서버 ──
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9

# ── 로컬 학생 모델 실행 ──
# 4.51 미만은 Qwen3 아키텍처를 모른다 — 기본 MODEL_ID(Qwen3-4B-Instruct-2507)가
# 로드 자체에서 실패한다. 구모델(Qwen2.5)로 되돌릴 때도 이 하한은 문제없다.
transformers>=4.51
accelerate>=0.27.0
sentencepiece
protobuf

# 그림자 모드(A-3) — 야간 학습이 만든 LoRA 델타를 이미 떠 있는 로컬 모델에
# 붙였다 떼는 데만 쓴다. 없으면 그림자만 조용히 쉬고 대화는 그대로다.
# 학습 자체는 이 venv가 아니라 trainingPythonPath의 인터프리터가 한다.
peft>=0.13

# ── PyTorch CUDA (RTX 3060 - CUDA 12.1) ──
# 아래 명령어로 별도 설치:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# ── TTS ──
# edge-tts: Microsoft Edge 신경망 음성 (무료, 키 불요, 네트워크 필요).
# PyInstaller 포함 대상이라 상한 핀 — aiohttp 등 전이 의존성 drift가
# 릴리스 빌드에서만 깨지는 사고 방지.
edge-tts>=7.2,<8
pyttsx3==2.90
soundfile==0.12.1
numpy==1.26.0

# ── 음성 복제 (캐릭터 목소리) ──
# seed-vc: 제로샷 음색 변환. 미설치 환경에서는 custom 음성 기능만 조용히
# 비활성화된다 (voice_clone_service.is_available 게이트). GPU 권장:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
# 주의: 패키징 exe(requirements-packaging.txt)에는 넣지 않는다 — 용량 수 GB.
seed-vc

# ── 기타 API (선택) ──
# httpx<0.28 : starlette TestClient는 httpx 0.28에서 제거된 `app=` kwarg를 쓴다
# (fastapi 0.109/starlette 0.35 기준). 백엔드 런타임에선 영향 없지만 pytest 픽스처
# (FastAPI TestClient)가 0.28+에서 깨지므로 함께 핀해둔다.
httpx>=0.27.0,<0.28
huggingface_hub>=0.21.0
anthropic>=0.18.0
groq>=0.4.0

# ── 임베딩 (장기 기억 / 파일 이해 / 웹 검색 결과 의미 비교) ──
# sentence-transformers는 첫 실행 때 모델(~90MB)을 HuggingFace에서 다운받아
# 캐시하고, 이후엔 오프라인으로도 동작한다. all-MiniLM-L6-v2가 기본.
sentence-transformers>=2.7.0

# ── 개발/테스트 (dist에는 포함 안 됨, requirements-packaging.txt 참고) ──
pytest>=8.0.0
pytest-asyncio>=0.23.0
