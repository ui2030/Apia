# Apia 구조 문서

> 데스크톱 위에 사는 3D 캐릭터 AI 비서.
> 캐릭터가 방 안에서 스스로 걷고 · 가구를 쓰고 · 표정과 입모양을 지으며 · 대화합니다.
> 이 문서는 특정 캐릭터/모델에 종속되지 않습니다. **어떤 모델이든 교체 가능**하다는 전제로 일반 용어로 작성했습니다.

---

## 1. 실행 구조 — 두 세계 + 다리 하나

Apia는 **Electron**(웹 기술로 데스크톱 앱을 만드는 틀)으로 되어 있어 코드가 두 세계로 나뉩니다.

```
┌──────────────────────────┐         ┌──────────────────────────┐
│   메인 프로세스            │         │   렌더러 (화면·두뇌)       │
│   electron/  "관리자"      │◀─IPC──▶│   src/     "무대·배우"     │
│                           │window.api│                          │
│ • 창 생성 / 월페이퍼 부착   │         │ • 방·캐릭터 3D 그리기       │
│ • AI 백엔드 켜고 끄기       │         │ • 걷기·표정·성격·행동       │
│ • 캐릭터 파일 저장 / 검증   │         │ • 채팅·음성·립싱크          │
└──────────────────────────┘         └──────────────────────────┘
            │
            ▼
   AI 백엔드(별도 프로세스)  ← 대화 응답 · 감정 · 음성(TTS) 생성
```

- **IPC**(프로세스 간 통신): 두 세계는 `window.api` 다리로만 대화합니다.
- **보안**: 렌더러가 요청할 수 있는 동작은 화이트리스트로 제한하고, 비밀 API 키는 값 대신 "있음/없음"만 전달합니다.
- 3D 캐릭터 포맷은 **VRM**(범용 아바타 규격)과 **PMX/MMD**(MikuMikuDance 계열) 둘 다 지원합니다.

---

## 2. 폴더 구조

```
Apia/
├─ electron/               메인 프로세스
│   ├─ main.js                 앱 진입점 · 전체 지휘
│   ├─ preload.js              렌더러↔메인 안전한 다리(bridge)
│   ├─ cornerPreload.js        구석 버튼 창 전용 다리
│   ├─ schemas.js              디스크에서 읽는 모든 파일의 검증 규칙
│   ├─ ipc/                    캐릭터 관련 IPC 채널 등록
│   └─ services/               창관리·월페이퍼·백엔드기동·레지스트리·임포트·설정
│
├─ src/                    렌더러 (화면과 두뇌)
│   ├─ main.js                 렌더 루프 + 전체 지휘
│   ├─ [씬/방]      sceneRuntime · world · furnitureLayout · propManager · modelRuntime
│   ├─ [애니메이션]  animationRuntime · inertialization · motionManager · motionAssets · poseRig
│   ├─ [행동두뇌]    characterController · behaviorDirector · activityRunner
│   │                needsManager · adaptationStore · touchInteraction
│   ├─ [얼굴/대화]   expressionRuntime · lipsyncRuntime · chat · chatRenderer
│   └─ assets/characters/      캐릭터별 프로필 · 모션 · 레지스트리
│
├─ scripts/                빌드 · 릴리스 · 검증 스크립트
└─ tests/                  단위 테스트 + GUI(Playwright) 테스트
```

---

## 3. 데이터 흐름 — 두 가지 관점

### (가) 매 프레임(초당 약 60회) 그려지는 순서
이 순서가 어긋나면 캐릭터가 깨지기 때문에, 코드에 "이 순서를 지켜라" 주석이 잔뜩 달려 있습니다.

```
클립 재생 → 뼈 부드럽게 잇기(인러셜라이제이션) → 몸 자세 계산
   → 손끝 IK → 걷기 다리 IK → 손 소품 붙이기 → 표정 / 입모양
```

### (나) 사용자가 말을 걸었을 때
```
채팅 입력 →(IPC)→ AI 백엔드 → 답변 + 감정 + 음성
   → 감정 표정 + 말풍선 + 카메라 바라보기 + 음성 재생 + 입모양 동기화
```

---

## 4. 서브시스템 상세

### ① 창 / 월페이퍼  (`electron/`)
투명한 캐릭터 오버레이 창, 설정 창, 독립 채팅 창, 구석 버튼 창을 만들고 관리.
**월페이퍼 모드** = 캐릭터를 바탕화면 아이콘 뒤 레이어에 붙이는 기능. 트레이 아이콘·전역 단축키·크래시 복구·모니터 위치 인식도 담당.

| 모듈 | 역할 |
|------|------|
| `main.js` | 앱 진입점, 모든 서비스를 연결하는 지휘자 |
| `windowManager` / `windowBoundsPolicy` | 창 생성·배치, 저장된 위치 정규화 |
| `wallpaperMode` | 바탕화면 뒤 레이어에 창 부착(2가지 전략 + 헬스체크) |
| `preload` / `cornerPreload` | 렌더러에 노출할 안전한 API 다리 |

### ② 백엔드 / 캐릭터 서비스  (`electron/services`)
두 개의 저장소 성격 도메인. 설계 원칙은 **"디스크 경계에서 검증·정규화하고, 렌더러는 절대 죽이지 않는다."**

| 모듈 | 역할 |
|------|------|
| `backendDiscovery` / `backendLifecycle` | AI 백엔드 포트 탐색 + 켜고/헬스체크/재시작/종료 |
| `backendEnvRepository` | API 키 파일 관리(비밀키는 "있음/없음"만 노출) |
| `registryService` | 설치된 캐릭터 목록(레지스트리) 읽기/쓰기 |
| `characterImportService` | zip/모델 파일 → 캐릭터 폴더로 풀어 등록 |
| `settingsAggregate` | 앱 설정 파일 정규화·기본값 병합 |
| `schemas` | 모든 디스크 파일의 형식 검증 규칙(스키마) |
| `ipc/registerCharacterIpc` | 위 기능들을 렌더러에 IPC로 노출 |

### ③ 씬 · 방 · 가구  (`src/`)
매 프레임 그리는 렌더 루프와 3D 무대.

| 모듈 | 역할 |
|------|------|
| `sceneRuntime` | 무대 구성(카메라·조명·방·가구·디오라마 앵글) |
| `furnitureLayout` | **가구의 단일 진실 원천**(위치·상호작용·활동 정의) |
| `world` | 가구를 클릭 가능한 대상 + 활동 시작점으로 변환 |
| `propManager` | 손에 든 소품(컵·잔·책)을 매 프레임 손 뼈에 추적 |
| `modelRuntime` | VRM/MMD/물리 엔진 지연 로딩, 모델별 안정화 처리 |

### ④ 애니메이션 · 리깅  (`src/`)
"모션 이름"을 실제 뼈 움직임으로 바꿈. 세 소스를 섞습니다.

| 모듈 | 역할 |
|------|------|
| `animationRuntime` | 제작된 클립 재생(포맷별), 부드러운 전환, 소유권 반납 |
| `inertialization` | 클립이 바뀔 때 뼈가 뚝 끊기지 않게 이어주는 보간 |
| `poseRig` | **절차적 포즈 엔진** — 호흡·시선·눈깜빡임·손짓·IK를 코드로 실시간 생성 |
| `motionManager` | 성격·기분에 따라 어떤 동작을 얼마나 자주 할지 선택 |
| `motionAssets` | 모션 이름 → 실제 클립 파일 경로 해석(없으면 절차적 대체) |

핵심 설계: 뼈를 **이름이 아니라 "역할(role)"** 로 다뤄서, 모델마다 뼈 이름이 달라도 동작합니다.

### ⑤ 행동 두뇌  (`src/`)
캐릭터의 의사결정. 상태 기계 위에 3층 결정 엔진이 올라갑니다.

| 모듈 | 역할 |
|------|------|
| `characterController` | 걷기·앉기·충돌 회피 상태 기계, 눈깜빡임·시선·앉은키 보정 |
| `needsManager` | 목마름·지루함·피곤 등 내부 욕구가 차오르고, 급한 걸 채울 활동 선택 |
| `behaviorDirector` | 느린 AI가 "기분"을 정해 규칙 기반 행동을 약하게 물들임(백엔드 죽어도 정상) |
| `activityRunner` | "걸어가서 앉아 마시고 치우기" 같은 다단계 활동을 순서대로 실행 |
| `adaptationStore` | 사용자의 하루 리듬·친밀도를 학습해 활기·걷기 비율을 미세 조정 |
| `touchInteraction` | 포인터 제스처(탭·쓰다듬기·잡기)를 분류 |

### ⑥ 얼굴 · 대화  (`src/`)

| 모듈 | 역할 |
|------|------|
| `expressionRuntime` | 감정 → 얼굴 모프(표정 변형), 자율 눈깜빡임·미세 표정 |
| `lipsyncRuntime` | 음성 분석 → 입모양 타임라인 생성 후 매 프레임 적용 |
| `chat` | 메인 창 채팅 + 음성 인식 + 음성 합성 + 클릭통과 관리 |
| `chatRenderer` | 독립 채팅 창(캐릭터를 IPC로 원격 구동) |

### ⑦ 캐릭터 데이터  (`src/assets/characters`)
캐릭터를 설명하는 디스크 데이터. **모델을 새로 넣으면 여기에 자동 생성**됩니다.

| 파일 | 역할 |
|------|------|
| `character_registry.json` | 설치된 캐릭터 목록 + 현재 활성 캐릭터 |
| `profile.generated.json` | 자동 도출된 성격 슬라이더(0~1)·말투·행동 성향·시스템 프롬프트 |
| `profile.user.json` | 사용자 수정(이름·성격 슬라이더 오버라이드 등) |
| `interpretation_presets.json` | 성격 "렌즈"(차분/활발 등 오프셋 프리셋) |
| `motions/index.json` | 캐릭터별 로컬 모션 오버라이드 |

---

## 5. 핵심 설계 계약 (건드릴 때 주의)

1. **가구 단일 원천** — `furnitureLayout.js`의 좌표 하나가 "보이는 위치 + 걸어가는 위치 + 앉는 오프셋"을 모두 결정. 그래서 시각과 클릭이 어긋나지 않습니다.
2. **모델 불문(역할 매핑)** — 뼈를 이름이 아닌 "역할"로 다뤄 어떤 모델이든 지원. 모든 역할은 없어도 되는 선택 사항.
3. **디스크 경계 검증** — 파일에서 읽는 모든 것은 스키마로 검사·복구. 하나가 깨져도 앱은 죽지 않고 기본값으로 버팁니다.
4. **프레임 순서 불변식** — 4장 (가)의 순서는 고정. 순서를 바꾸면 자세가 깨집니다.

---

## 6. 알려진 한계 / 정리 대상

- **캐릭터 데이터의 일반화** — 프로필/폴더에 특정 고유명사가 하드코딩된 부분이 있어, 모델 교체 전제에 맞게 일반 용어로 치환 필요.
- **레지스트리 이원화** — 실제로 읽는 레지스트리 파일 외에 오래된 중복 파일이 있고, 그 안의 텍스트 인코딩이 깨져 있음.
- **경로 이식성** — 활성 캐릭터 항목이 특정 PC 절대경로를 저장해, 다른 PC나 설치본에서 깨질 수 있음.
- **저장 안정성** — 일부 저장 경로가 원자적(임시파일+교체) 방식이 아니라, 저장 중 강제 종료 시 손상 위험.
- **채팅 코드 중복** — 메인 창과 독립 창 채팅 로직이 상당 부분 복제되어 있고, 두 창의 대화 이력이 동기화되지 않음.
- **미완성/휴면 코드** — 걷기 클립, 손가락 굴곡 표현 등 일부 기능이 비활성 상태로 남아 있음.

---

## 7. 용어집 (비개발자용)

| 용어 | 뜻 |
|------|-----|
| Electron | 웹 기술로 데스크톱 앱을 만드는 틀 |
| 렌더러 / 메인 프로세스 | 화면을 그리는 쪽 / OS·창을 다루는 쪽 |
| IPC | 두 프로세스가 주고받는 통신 통로 |
| VRM / PMX(MMD) | 3D 캐릭터 파일 포맷 두 종류 |
| 클립 (VRMA/VMD/FBX) | 미리 제작된 동작 애니메이션 파일 |
| 모프(morph) | 미리 만들어 둔 표정/형태 변형 |
| IK(역운동학) | 손끝 목표를 주면 팔꿈치 각도를 역산하는 기법 |
| 인러셜라이제이션 | 동작이 바뀔 때 부드럽게 이어주는 보간 |
| 절차적(procedural) | 파일이 아니라 코드로 실시간 생성하는 방식 |
| TTS / STT | 글자→음성 / 음성→글자 변환 |
| 스키마(schema) | 파일이 올바른 형식인지 검사하는 규칙 |
