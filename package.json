{
  "name": "apia",
  "version": "1.0.0",
  "description": "Apia - Desktop AI Assistant",
  "author": "ui2030",
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently \"vite\" \"wait-on http://localhost:5173 && electron . --dev\"",
    "build": "vite build",
    "build:backend": "node scripts/build-backend.mjs",
    "start": "electron .",
    "test": "vitest run",
    "test:gui": "playwright test",
    "verify": "npm run build && node scripts/verify-build.mjs && node --check electron/main.js && node --check electron/preload.js && node --check electron/ipc/registerCharacterIpc.js && node --check electron/services/registryService.js && node --check electron/services/characterImportService.js && npm test",
    "verify:release": "node scripts/verify-release.mjs",
    "smoke:release": "node scripts/smoke-release.mjs",
    "dist:dir": "npm run build && npm run build:backend && npm run verify:release && node scripts/run-release-builder.mjs --dir",
    "dist:win": "npm run build && npm run build:backend && npm run verify:release && node scripts/run-release-builder.mjs --win nsis"
  },
  "dependencies": {
    "@pixiv/three-vrm": "^3.5.1",
    "@pixiv/three-vrm-animation": "^3.5.2",
    "adm-zip": "^0.5.17",
    "electron-as-wallpaper": "^1.0.8",
    "extract-zip": "^2.0.1",
    "three": "^0.164.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "concurrently": "^8.2.2",
    "electron": "^28.3.3",
    "electron-builder": "^26.8.1",
    "playwright": "^1.60.0",
    "vite": "^6.4.3",
    "vitest": "^4.1.8",
    "wait-on": "^7.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
