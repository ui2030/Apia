node_modules/
dist/
release/
backend-dist/
backend-build/
*.log
.env

# Step 6 — Mixamo .fbx clips are user-supplied. Adobe TOS allows personal
# use but redistribution is grey, so keep user-side .fbx out of git.
# manifest.json + README.md inside the folder are tracked (they describe
# the contract); only the binaries are excluded.
src/assets/motions/fbx/**/*.fbx
src/assets/motions/fbx/**/*.FBX

# /goal Step 5 — .vmd idle pack from Deedee524 (DeviantArt) ships under
# "use freely, credit me, do NOT redistribute original motions". The
# LICENSE-<author>.txt files inside the folder are tracked (they carry
# the author's permission statement); the .vmd binaries are not.
src/assets/motions/vmd/**/*.vmd
src/assets/motions/vmd/**/*.VMD
# …but Apia-generated original clips (scripts/gen-vmd.py) carry no license
# restriction and are model-agnostic (gesture deltas; the per-model arm hang is
# composed at runtime), so ship them. Whitelist each generated file.
!src/assets/motions/vmd/talk/explain.vmd
!src/assets/motions/vmd/talk/think.vmd
!src/assets/motions/vmd/talk/soft.vmd
!src/assets/motions/vmd/talk/happy.vmd
!src/assets/motions/vmd/talk/neutral.vmd
!src/assets/motions/vmd/talk/explain_soft.vmd
!src/assets/motions/vmd/react/happy.vmd
!src/assets/motions/vmd/react/shy.vmd
!src/assets/motions/vmd/idle/arms_crossed.vmd
!src/assets/motions/vmd/idle/hand_on_hip.vmd
!src/assets/motions/vmd/idle/hands_clasped.vmd
!src/assets/motions/vmd/idle/relaxed.vmd
!src/assets/motions/vmd/idle/ponder.vmd
!src/assets/motions/vmd/idle/hands_back.vmd

# Local-only / generated artifacts that must never reach the public repo.
backend/.venv/
backend/apia.db
checkpoints/
backend/checkpoints/
mixamo-fbx/
# Debug render screenshots named after a character IP — keep out of a public repo.
kisaki/

# Third-party character model assets (copyright — redistribution not permitted).
# Kept on local disk so the app runs, but never tracked in the public repo. The
# engine is model-agnostic; per-user models live locally only.
src/assets/Aru/
src/assets/Hina_Swimsuit/
src/assets/Kisaki/
src/assets/Kisaki_Uniform/
src/assets/Mari_Idol/
src/assets/Mika/
src/assets/Vanya/
src/assets/nozomi-hikari/
src/assets/characters/
src/assets/character_registry.json
